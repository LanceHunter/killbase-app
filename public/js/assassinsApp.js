(function() {
  console.log("You see me!");

/*
  let searchField = $('#seach');
  let searchButton = $('#searchButton');
  let searchOptions = $('#searchOptions');

  searchButton.click(() => {
    if (searchField.val() !== '') {
      if (searchOptions.val() === 'Search by Name') {
        console.log("Search field value - " + searchField.val());
        $('#assassinModal').removeClass('hide');
        $('#assassinModalBody').load(`/assassins/names/${encodeURIComponent(searchField.val())}`);
      }
      if (searchOptions.val() === 'Search by Code Name') {
        console.log("Search field value - " + searchField.val());
        $('#assassinModal').removeClass('hide');
        $('#assassinModalBody').load(`/assassins/codenames/${encodeURIComponent(searchField.val())}`);
      }
    }
    event.preventDefault();
  })

  $('#modalClose').click(() => {
    $('#assassinModal').addClass('hide');
  });

*/
})();
